# QA-Report: M0 — Repo, branding, build green

Status: **Reviewed** (smoke test passed; awaiting final Codex GREEN)
Date: 2026-06-23
Branch: `feature/m0-fork-branding` (base `origin/main` pinned to tag `v0.96` = `10ab2f3`)

## Scope
M0 of [the plan](2026-06-23-Plan-Divvy2-Window-Snapper.md) §5: fork Rectangle, pin
the base, overlay the divvy2 skeleton, build green, apply branding, disable Sparkle,
and confirm the app launches / grants Accessibility / snaps windows.

## Environment
- Xcode 26.5 (17F42), Swift 6.3.2, macOS 26.5.1 (25F80), arm64.
- `security find-identity`: **0 valid signing identities** → ad-hoc (`-`) signing.
- Note: a one-time `sudo xcodebuild -runFirstLaunch` was required to repair a stale
  `DVTDownloads.framework` vs. `IDESimulatorFoundation` mismatch that blocked all
  `xcodebuild` invocations (exit 70).

## What shipped in M0
- **Fork & pin:** `perg593/Rectangle`; `origin/main` force-pinned to tag `v0.96`
  (`10ab2f3`); `upstream` = `rxhanson/Rectangle` added for rebases.
- **Identity:** bundle id `com.perg593.divvy2`; launcher `…​.RectangleLauncher`;
  tests `…​.RectangleTests`. App/product name **"Moving Van"** (Moving Van.app,
  CFBundleDisplayName/CFBundleName). Swift module name **pinned to `Rectangle`**
  (PRODUCT_MODULE_NAME) so XIB/storyboard `customModule` refs and
  `@testable import Rectangle` keep resolving.
- **Sparkle disabled:** removed SUFeedURL/SUPublicEDKey/SUScheduledCheckInterval;
  removed the `SPUStandardUpdaterController`/delegate code; update menu + prefs
  controls hidden and actions no-op'd. (Sparkle SPM dep remains linked-but-unused.)
- **Signing:** all configs (Debug **and** Release) ad-hoc `-`, `DEVELOPMENT_TEAM=""`.
- **Collision hygiene:** URL scheme `rectangle` → `divvy2`; auto-config support dir
  `Rectangle`/`RectangleConfig.json` → `Moving Van`/`MovingVanConfig.json`.

## Build results (QA gate)
| Build | Result |
|---|---|
| `xcodebuild … -configuration Debug build` | ✅ BUILD SUCCEEDED |
| `xcodebuild … build-for-testing` | ✅ TEST BUILD SUCCEEDED |
| `xcodebuild … -configuration Release build` | ✅ BUILD SUCCEEDED (Signature=adhoc, TeamIdentifier not set) |

Built-app verification: BundleID `com.perg593.divvy2`, name/display "Moving Van",
executable "Moving Van", embedded launcher `com.perg593.divvy2.RectangleLauncher`,
no `SUFeedURL`, URL scheme `divvy2`, Signature=adhoc.

## Smoke test (the M0 acceptance BLOCKER)
- **Launch:** ✅ `Moving Van.app` (Debug, ad-hoc) launches as a menu-bar app.
- **Accessibility grant:** ✅ granted to `com.perg593.divvy2` via System Settings.
- **Window snap:** ✅ snapping a focused window (menu-bar → "Left Half") **moved the
  window correctly** (user-confirmed, 2026-06-23).

## Codex checkpoint review
- Round 1 (commit `3070b37`): **NOT GREEN** — 1 BLOCKER (smoke test pending),
  3 MAJOR (Sparkle surface still live, Release signing, URL scheme collision),
  2 MINOR (config dir, AX prompt string), 1 NIT (scheme name). Module-name pinning
  and the v0.96 pin were explicitly cleared.
- Fixes landed in commit `e43f294`; smoke test completed.
- Round 2: _pending — re-run after this report; target VERDICT: GREEN before M0.5._
